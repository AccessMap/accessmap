version: '3.2'

services:

  api:
    restart: always

  webapp:
    environment:
      - OPENID_CLIENT_ID=AccessMap

  caddy:
    restart: always
    volumes:
      - ./config/Caddyfile.prod:/etc/Caddyfile
      - /docker/.caddy:/root/.caddy
    ports:
      - 80:80
      - 443:443
